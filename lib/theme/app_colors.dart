import 'package:flutter/material.dart';

class AppColors {
  // 기본 배경색
  static const Color background = Color(0xFF121212);

  // 텍스트 색상
  static const Color textPrimary = Color(0xFFC8C8C8);
  static const Color textSecondary = Color(0xFF6B6B6B);

  // 버튼 색상
  static const Color buttonPrimary = Color(0xFFC3C3C3);
  static const Color buttonSecondary = Color(0xFF3F3F3F);

  //컨테이너 색상
  static const Color containerPrimary = Color(0xFF252525);

  // 네비게이션 바 색상
  static const Color navBar = Color(0xFF171717);
}
